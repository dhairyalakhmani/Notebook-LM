# Entry point / CLI for the notebook.
#
# Target commands (see STEPS.md):
#   python main.py add <notebook> <file-or-url>   -> ingest a source
#   python main.py sources <notebook>             -> list what is in the notebook
#   python main.py ask <notebook> "question"      -> grounded answer with citations
#
# Nothing is implemented yet. Phase 1 fills in `add`, Phase 6 fills in `ask`.
