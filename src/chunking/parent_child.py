# Phase 2. Turns Sections into two linked levels:
#   child  = small, precise -> this is what gets embedded and searched
#   parent = the surrounding block -> this is what gets sent to the LLM to read
# Matching small but reading big is what stops answers from losing context.
