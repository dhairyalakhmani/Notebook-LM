export const SNIPPETS: Record<number, string> = {
  46: "computer networks necessarily constrain throughput",
  47: "The time required to examine the packet's header and determine where to direct the packet is part of the processing delay",
  48: "The propagation delay is the distance between two routers divided by the propagation speed",
  50: "The most complicated and interesting component of nodal delay is the queuing delay",
  410: "routed to an area border router (intra-area routing), then routed through the backbone",
  412: "pairs of routers exchange routing information over semi-permanent TCP connections",
  415: "address of a router that does not belong to AS1",
};
